// pedir la edad y si es mayor a 18 a;os indica que ya puede conducir //

var age = prompt("how old are you");

if(age >= 18){
    document.write("you should driver");
}
else {
   document.write("you can't drive yet");
}